// An object for storing certain game data
// Json format for storage/loading
var init_gamestate = {
    playerRep: 0,
    userName: "default"
};

// Loads a preexisting game state from local cookie
function loadGame()
{
    
}

// set to initial, or to a loaded state
var gamestate = init_gamestate;

describe("Game state", function(){
 it("can be loaded");   
});